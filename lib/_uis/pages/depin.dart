import 'package:flutter/material.dart';

class DepinPage extends StatelessWidget {
  const DepinPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('Depin page');
  }
}
